checks:
  golint:
    filters:
      - value: "should have comment or be unexported"
      - value: "or a comment on this block"
      # type actually provides configuration, so stutter is OK
      - value: "type name will be used as config.ConfigProviderLocator"
